{
  "name": "@inkin/core",
  "version": "0.5.0",
  "description": "Editable React diagrams from a typed schema. v0.5.0 adds animated data-flow tokens on top of 0.4.x's editor chrome: declare a `flows` field on the Diagram and one `<circle>` per flow traverses its composed path using native CSS `offset-path` keyframes — no animation library, honors `prefers-reduced-motion`. Editing primitives, Inspector/Palette chrome, and the framework-agnostic schema kernel at @inkin/core/schema are unchanged.",
  "license": "MIT",
  "author": "kartikeya27",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kartikeya-27/inkin.git"
  },
  "type": "module",
  "engines": {
    "node": "^22.18.0 || >=24.0.0"
  },
  "sideEffects": [
    "**/*.css"
  ],
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./schema": {
      "types": "./dist/schema/index.d.ts",
      "import": "./dist/schema/index.js",
      "require": "./dist/schema/index.cjs"
    },
    "./styles.css": "./dist/styles.css",
    "./diagram.schema.json": "./dist/schema/diagram.schema.json",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown && node scripts/emit-json-schema.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium firefox webkit",
    "typecheck": "tsc --noEmit",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write",
    "size": "size-limit",
    "attw": "pnpm dlx @arethetypeswrong/cli --pack",
    "verify": "pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm size",
    "prepublishOnly": "pnpm verify",
    "examples": "pnpm --filter examples dev"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-dom": ">=18.0.0"
  },
  "dependencies": {
    "@dagrejs/dagre": "^3.0.0",
    "@xyflow/react": "^12.11.0",
    "html-to-image": "^1.11.0",
    "zod": "^4.4.3",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@playwright/test": "^1.60.0",
    "@size-limit/preset-small-lib": "^11.1.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/react": "^16.0.0",
    "@tsdown/css": "^0.22.1",
    "@types/node": "^25.9.1",
    "@types/react": "^19.2.16",
    "@types/react-dom": "^19.0.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "jsdom": "^29.1.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "size-limit": "^11.1.0",
    "tsdown": "^0.22.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "keywords": [
    "diagram",
    "react",
    "react-component",
    "schema",
    "zod",
    "xyflow",
    "react-flow",
    "mermaid",
    "graph",
    "editable",
    "ai",
    "json-schema",
    "state-machine",
    "architecture"
  ]
}
